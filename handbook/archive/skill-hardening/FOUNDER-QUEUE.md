# Founder decision queue — plan-21 design run

*Archived 2026-09-05. Rows marked OPEN are not live tracking: the brand call is D29 in `DECISIONS.md`, S6-11 is in `STATUS.md` → Deferred, everything else was handled by later work or parked with the bot.*

Genuinely-ambiguous TASTE calls the known rulings don't settle, surfaced during the autonomous
run. For each: the most mock-faithful default was chosen and the build PROCEEDED (never blocked);
the founder reviews these later. Known rulings already applied (NOT queued): NPA = calm violet
(not red); "steal the styling, ignore the data"; responsive (not app-only); `direction-a` is the
target; content is schema-driven; adapter is Pulumi.

| # | Surface | Question | Default taken (mock-faithful) | Status |
|---|---------|----------|-------------------------------|--------|
| 1 | TECH-06 oklch fallback (Phase 1) | The oklch fallback was slimmed to a `@supports not (color: oklch())` block only (retiring the redundant hex-first `:root` restatement). alignment-review surfaced: does this match TECH-06's intent for the oldest Thai-Android WebViews? | **Proceeded with `@supports not(oklch)`.** Verified technically sound: `@supports` ships since Chrome 28 (2013), `oklch()` since Chrome 111 (2023) — the gate covers the ENTIRE realistic pre-oklch Android range; a device too old for `@supports` predates the oklch cutoff by ~10yr. Low risk; confirm against §5.8 (beta Android vintage) if data arrives. | OPEN (low) |
| 2 | Card density: Thai body size (pass 1) | The `direction-a` mock renders card location/specs at 11px. TH-06/07 + theme rule say Thai body is ≥13px with line-height ≥1.6 (looped-Thai readability). The two conflict. | **Rendered Thai body at 13px (`text-sm`) + leading 1.625** — honoring the register over the mock's pixel size (Thai readability > matching the mock exactly). Cards are slightly denser/taller than the mock as a result. Numerals/price stay tight. Confirm the readability-over-compactness call. | OPEN (low) |
| 3 | Detail/card LINE CTA colour | The mock's primary CTA ("Chat on LINE") is trust-blue; our `LineCtaButton` is LINE-green (`--color-line` #06c755). | **Kept LINE-green** per CONV-06 + the taste brief ("explicitly LINE-branded chat CTA") — the mock's blue is generic primary styling. Settled by the brief, surfaced for confirmation. | OPEN (low) |
| 4 | Brand wordmark (pass 2 header) | The mock header has a compact wordmark ("ทรัพย์ดี"); we have no settled brand name. | **Used a house icon + the existing site title** (`home.title` = "ประกาศอสังหาริมทรัพย์") rather than invent a brand name (guardrail: don't add content the schema lacks). Want a short brand wordmark / logo? Decide the brand name. | OPEN |
| 5 | Header search placement (pass 2) | The mock puts the search INSIDE the sticky header; ours sits in the FilterBar pill just below the header. | **Kept search in the FilterBar** to avoid splitting the search island (search text + chips share one island/navigation). Functionally equivalent, prominent at the top. Move into the header bar if desired (small island refactor). | OPEN (low) |
| 6 | No explicit WCAG-contrast heuristic in the register (pass 3) | The register §4 has no a11y/contrast heuristic; a real dark-mode AA failure (white-on-light-blue filled CTAs) was caught only by an audit + the new `assertCtaContrast` deterministic invariant (F4), not by any register ID. | **Enforced via the F4 e2e invariant** (filled CTAs ≥4.5:1 in all modes). Whether to ADD an explicit a11y/contrast heuristic to the register §4 is a founder call (register change). The deterministic net holds regardless. | OPEN |

## Stage 5 (MINI App rebuild) — taste/scope calls surfaced during the orchestrated build

Same discipline: mock-faithful default taken, build PROCEEDED, founder reviews later. Known Stage-5 rulings already applied (NOT queued): edit-by-reply OUT (A3a); DF-6 descoped (consistent with A3a); claim/publish = the only public path (D7); group-private = source group only; `/` = my-listings home; LINE-green CTA where a buyer-facing chat CTA appears.

| # | Surface | Question | Default taken (mock-faithful) | Status |
|---|---------|----------|-------------------------------|--------|
| S5-1 | TH-03 trust signal on the my-listings CRM card (Build B) | TH-03 wants a human trust signal (poster name/handle) on "every card". On the **owner's own** CRM card the poster IS the viewer, so "ลงประกาศโดย คุณ" may be redundant noise — but a broker managing multiple owners' listings would want attribution. | **Omitted** the poster row on the owner CRM card (the slim card DTO carries no `postedByName`; the owner is the poster). Exempt the CRM-owner view from TH-03, or enforce it (add `postedByName` to the card DTO + render)? | OPEN |
| S5-2 | Detail gallery photo count (Build B, CONV-03) | The detail swipe gallery shows no "N รูป" count overlay; the Stage 5 mock doesn't show one either. CRM detail is less discovery-oriented than the public card. | **No count overlay** (mock-faithful). Add a low-friction count if owners want it? | OPEN (low) |
| S5-3 | Privacy-notice link in the LIFF SPA (Build B, LEGAL-04) | The bot shows the PDPA group-join disclosure; the LIFF SPA chrome has no link to the privacy notice. | **No link in the CRM chrome** (Build B is read-only browse of own listings). PDPA best practice would add a footer link — decide before launch. | OPEN |
| S5-4 | Deed badge + restricted-transfer warning on detail (COPY-04) | `ListingDetailDto` carries no deed-type field, so the COPY-04 deed badge isn't rendered on the mini-app detail. | **Deferred to the Build-C/D DTO extension** — flag so the deed badge isn't missed when the detail DTO grows. | OPEN (Build-C checklist) |
| S5-5 | my-listings card fidelity vs mock (Build B perceptual pass) | The lifecycle left-accent stripe IS present (`border-l-[3px]`) but reads thinner than the mock; the stats strip is 4 text cells (no icons) vs the mock's 5 icon tiles; the mock's user-identity row (avatar + group code) is omitted. Deterministic styling (theme/TH-07/contrast/dark) all green. | **Shipped mock-faithful-enough** (structurally faithful per the perceptual review; content extras like expiry-date rows are correctly schema-driven/omitted). Thicken the stripe / add stat icons / add an identity header? All low-friction polish — founder taste. | OPEN (low) |
| S5-6 | Claim CTA copy "เผยแพร่สาธารณะเลย" (Build C, COPY-02) | The publish CTA carries the emphatic particle "เลย" — warm/encouraging but COPY-02/B3-F08 prefer bare verbs ("เผยแพร่สาธารณะ"). | **Kept the warm form** (matches the non-bureaucratic tone). Drop the particle for strict COPY-02? Low stakes. | OPEN (low) |
| S5-7 | Deed type / "verify the extraction" on the claim screen (Build C, COPY-04/LEGAL-06) | The claim screen's LEGAL-06 banner tells the poster to "check the details are correct before claiming/publishing," but the listing summary shows only headline/price/location — NOT the deed type (which the bot may have mis-extracted). Owners CAN correct fields post-claim via the my-listings edit surface (Build D). | **Shipped the compact summary** (streamlined claim flow; post-claim edit is the correction path). Add a "ดูรายละเอียดทั้งหมด" (view full `/p/{id}`) link or a deed row on the claim screen so the poster can verify before publishing? Medium. **RESOLVED in INC-4:** the claim review step now shows a 3-step stepper + a structured spec FieldList + a "ดูรายละเอียดทั้งหมด" verify link → the full `/p/{id}` detail, so the poster can verify the bot's extraction before publishing. The deed-type ROW itself still defers to the S5-4 DTO field. | RESOLVED (verify link; deed row → S5-4) |
| S5-8 | Claim DM doesn't name the source group (Build C, FQ-3) | A broker active in several LINE groups gets a claim DM that doesn't say which group the listing came from (the headline is the only signal). Group `name` rows aren't populated until Stage 6 group-management. | **Omitted** (no group names yet). Add a "กลุ่ม: {name}" row to the DM card once group names exist. | OPEN (low; Stage-6-dependent) |
| S5-9 | Trust signal on the SAVED card (Build D, TH-03) | TH-03 wants a human trust signal on every card. The slim card DTO has no `postedByRole`/owner-direct field, so saved (third-party) cards show none — more pertinent here than on the owner's own my-listings cards (S5-1). | **Omitted** (no DTO field). Add `postedByRole`/`isOwnerDirect` to the card DTO + a provenance pill? Needs an api change + a density-vs-trust design call. | OPEN |
| S5-10 | "นัดดูทรัพย์" vs canonical "นัดชม" (Build D, B3/COPY-02) | The book-viewing CTA/labels use "นัดดูทรัพย์" (appoint+view+property — well-formed); the B3 glossary canonical is "นัดชม". B3's particle rule is flagged single-source/weak. | **Shipped "นัดดูทรัพย์"** (clear, consistent across the flow). Ratify it as brand vocabulary (update B3) or align to "นัดชม"? Low. | OPEN (low) |
| S5-11 | LEGAL-06 "verify independently" missing on the WEBSITE public cards (cross-cutting) | Register §4: LEGAL-06 "poster-provided, verify independently" must be visible on cards. Build D adds it to the mini-app `MyListingCard`, but the **public website** listing cards (BrowseResults/HomePage) appear to lack it — the higher-priority buyer-facing surface. | **Fixed on the mini-app cards**; the website-card gap is a separate website follow-up (a muted disclaimer line on the public listing card). | OPEN (website follow-up) |
| S5-12 | Photo-count chip shows "มีรูป" not "N รูป" (my-listings home, photo-forward pass) | The Stage-5 mock's photo-forward card carries a "📷 N รูป" count chip. The slim card DTO (`toCardDto`) carries `heroUrl?` but **no photo count** — so a real number can't be rendered without inventing data (guardrail: don't add content the schema lacks). | **Shipped a "📷 มีรูป" has-photos indicator** on cards that have a hero (omitted otherwise — no empty/zero chip). Mock-faithful in placement/treatment; honest about the count. To show "N รูป", add a `photoCount` to the card DTO (api+db change) — then the chip renders the number. Low; founder call. | OPEN (needs DTO field) |
| S5-13 | Asking-price / negotiable framing on the OWNER's own CRM card (alignment CONV-09/COPY-11) | The alignment review flagged that the SALE price on `MyListingCard` reads as "final" — no `listing.priceAsking` frame, no `ต่อรองได้` (negotiable) badge — though both strings exist (`display.ts`/i18n). CONV-09/COPY-11 forbid implying price finality in the negotiation-heavy Thai market. | **Held: NOT added to the owner CRM card** (the owner set their own price; asking/negotiable framing is a buyer-trust signal, primarily a buyer-facing concern — the public website cards + the detail screen). Register-applicability call: does CONV-09/COPY-11 apply to the owner's own management card, or only buyer-facing surfaces? | OPEN |
| S5-14 | My-listings card FORM — photo-forward (direction-a) vs the Stage-5 dashboard mock | The shipped card follows `direction-a-baania-clean` (large vertical hero, photo-forward — matching the goal's "photo-forward cards (large hero)"); the `explore-stage5-2-mylistings.html` mock card is denser/horizontal (small thumb + side status pills + a "ดู/เก็บ/นัด" engagement-metric row). | **Shipped the photo-forward direction-a language** (on the goal's stated wording). Which is the canonical bar for the my-listings cards? The mock's engagement-metric row also needs view/save/viewing counts the card DTO doesn't carry. | OPEN |

### Stage-5 my-listings PHOTO-FORWARD home (this increment) — resolutions of S5-1 / S5-5 / S5-9

- **S5-5 (card fidelity / stripe / stat icons / identity header) — substantially RESOLVED.** This increment thickened the lifecycle left-accent stripe to **4px** (`border-l-4`, was `border-l-[3px]`), split the stats strip into the mock's **5 tiles** (total / live / under-offer / draft / closed — under-offer is now its own tile), and added the **identity chrome** (avatar from the LIFF `pictureUrl` with an initial-based fallback, display name, source-group **code** row, and the "ทรัพย์ดี" wordmark). NOTE: the mock's stat tiles are coloured count + label with **NO icons** (matched exactly — text tiles, per-tile lifecycle tint); if the founder wants stat-tile glyphs that is an additive taste call. The mock's per-card freshness/expiry/interest/action rows are intentionally still omitted (correctly schema-driven — those fields aren't on the card DTO).
- **S5-1 (TH-03 poster name on the owner CRM card) — held OMITTED (unchanged).** The photo-forward card still does not render "ลงประกาศโดย …": the owner IS the viewer and the slim card DTO carries no `postedByName`. The default holds; the broker-managing-multiple-owners case (which would want attribution) still needs a DTO field + an api change — founder call, queued.
- **S5-9 (trust signal on the SAVED card) — NOT in this increment's scope.** The saved card is owned by `SavedPanel`/its card (a separate increment). Still OPEN as written (needs `postedByRole`/`isOwnerDirect` on the card DTO).
- **FQ-4 brand wordmark — provisionally taken as "ทรัพย์ดี"** (the working wordmark the Stage-5 mock carries) on the CRM home identity header. No brand name is settled (FQ-4 OPEN); chosen to render the mock's wordmark slot rather than leave it blank. Replace once a brand name is decided.
- **Group attribution — the identity-row group line was CUT (post-review).** It rendered the source-group code from a `groupCode` field that the real LIFF profile never returns (only the e2e mock set it) — dead-in-production "config nobody sets" / test theatre, flagged by the increment review. Removed for now (avatar + display name + wordmark, all real LIFF data, remain). **Stage-6 re-add:** restore a group label to the identity chrome once the source group is genuinely threaded into the user's profile (group names land with Stage-6 group management — consistent with S5-8).

## Stage 6 (Groups & Dealflow) — strategy defaults built in the overnight run (founder review)

The founder waived the pre-build approval gate for Stage 6 in this run; these are the **smallest-defensible
defaults** taken to unblock the build, each behind a clean seam. Full rationale in
`plans/19-v2-marketplace-rebuild/stage-6-groups-dealflow.md` §Resolved decisions (D-S6-1…9). Reverse/tune any.

| # | Decision point | Default taken | Founder call |
|---|---|---|---|
| S6-1 | Exclusivity window length | **7 days, per-group configurable** (D8); minimal admin number-field | Right for the Thai broker market? Richer per-deal-type tuning? | 
| S6-2 | Exclusivity state model | **DERIVED in the domain engine** (open/interest-flagged/lapsed/released/extended) from the existing `[held\|releasable\|released]` + expiry + flags; no enum migration | OK, or want explicit persisted states? |
| S6-3 | Interest-flag semantics | **Non-binding signal**, all flags shown newest-first, no priority queue, doesn't block early release | Add hold/notify/priority semantics? |
| S6-4 | Release mechanics + ignore behavior | Bot DM offers **release-publicly / release-to-other-groups / extend**; **ignored → stays group-private (NO silent auto-release)**; release-to-other-groups v1 = drop the exclusive mandate (no per-target-group plumbing) | Add a grace-period auto-release? Per-target-group release? |
| S6-5 | Admin surface + auth | **Inside the mini-app, `admin`-role gated, SERVER-enforced** — sidesteps the D19 domain / 4.4 LINE-Login dependency | Mini-app admin, or a future website `/admin`? |
| S6-6 | Quick-sale matching criteria | **Smallest overlap behind `matchVettedUsers` seam**: approved-vetted brokers/investors whose **stated preferences** (collected in the role-application: province + property-type + price-band) overlap the listing; server-side vetted filter | Add weighting / radius / deal-history ranking? Confirm preferences-in-application is the right capture point. |
| S6-7 | Moderation queue scope | **Minimal approve/reject** over `moderation_item WHERE pending` (not full CRUD); approve → listing active | Expand to a full moderation panel? |
| S6-8 | Vetting rejection + reviewer | 0009 adds `rejected` to approvalStatus + `reviewed_by`/`reviewed_at` to role | OK? |
| S6-9 | Rental renewal loop (D26) | **NOT built** (sales-first per D26) — queued | Confirm fast-follow, not this stage. |
| S6-10 | Migration 0009 + staging apply | 0009 = roleKind+`admin`, approvalStatus+`rejected`, role reviewer cols, minimal `broker_preference`. **Applying 0009 to staging RDS is FOUNDER-GATED** (written + Docker-PG-tested; staging `db:migrate` awaits founder) | Run the staging migrate when ready. |
| S6-11 | Moderation "approve → publicly-visible" BLOCK is DEFERRED | v1 moderation is admin REVIEW only (list pending + approve/reject the `moderation_item`). Nothing reads `moderation_item.status` in the claim/publish/public-query path, so a gate-failed listing isn't actually BLOCKED today and "approve" changes nothing observable. Wiring the block (a NOT-EXISTS-pending-moderation predicate on the public listing query + a publish guard) is **cross-cutting into the website/publish path** — out of the minimal-api scope. | Acceptance-criterion "approving sets the listing to active" needs this. Build the block (small public-query predicate + publish guard) as a fast-follow? |
| S6-12 | Quick-sale is a BOOLEAN, register wants a ≥3-tier urgency selector (alignment **DIST-11**) | a3 §DIST-11 wants a "seller urgency" selector with ≥3 tiers (standard / motivated / needs-to-close-within-4-weeks) feeding `urgency_indicator`. The Stage-1 schema's `urgency` enum is `[normal\|quick_sale\|price_reduced]` — NOT those tiers; the v1 toggle uses the existing `quick_sale` flag (smallest-defensible on the existing schema). **Rebuttal:** the boolean is the minimal version; the 3-tier `urgency_indicator` model needs a new schema/enum + selector. | Build the 3-tier urgency selector (needs a schema model), or ratify the boolean v1? |
| S6-13 | Quick-quote flow shows no discount-to-close EXPECTATION (alignment **DIST-04**) | DIST-04's anti-hype FLOOR (no "sell instantly" promise) IS met — the copy is non-hype ("invite vetted brokers to compete"). The missing piece is a displayed discount-to-close *expectation*; the numeric curve is **register-TBD (§5.7 / DF-7)**. The quote form DOES capture `discountVsMarket` (the data the future expectation needs). **Rebuttal:** the expectation display is blocked on the TBD curve. | Define the discount-to-close curve (§5.7/DF-7) → then add the expectation display? |
| S6-14 | Detail DTO doesn't surface `urgency` → quick-sale badge is session-local | After an owner toggles quick-sale, the badge persists server-side (proven by the broker-quote gate), but on a fresh detail reload it shows the toggle as un-toggled (the detail DTO carries no `urgency`). Idempotent/harmless, but no "already quick-sale" confirmation on reload. Fix = add `urgency` to the detail DTO (db `getPortalListingDetail` + api detail mapper + miniapp types + the UI reads it). Cross-cutting (db+api+miniapp). | Add `urgency` to the detail DTO (small) for the persistent badge? |
| S6-15 | Broker `/quote/{id}` screen has no listing summary | A pushed vetted broker is NOT a group member → the detail GET 404s → the quote screen renders form-only (no headline/price summary for the broker to quote against). Documented + intended (membership gate). | Give a pushed broker a minimal membership-independent listing summary (headline/price) on the quote screen? |
| S6-16 | No in-app DISCOVERY for `/apply` or the admin screens | The Stage-6 mini-app routes `/apply` (role application) + `/admin/vetting` + `/admin/moderation` are **deep-link-only** — there is deliberately no nav link (the admin gate is server-authoritative, so a nav link for non-admins would be odd UX). So today a user can't find "apply to be a broker," and an admin reaches the queues only via a known URL. | Add an admin-only nav entry / a rich-menu "Admin" tab (server-gated) + a discoverable "apply for broker/investor" entry point? |
