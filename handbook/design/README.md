# Design (decided — reference for building, not an open question)

- `design-direction.md` — the pick: Direction A "Baania-clean" trust-blue (founder, 2026-06-13). Live tokens are in `packages/ui/theme.css`, not here.
- `mockups/` — the visual bar for pages ("steal the styling, ignore the data"); `renders/` holds screenshots of the mocks for review.
- `tokens-candidates.md` — the three candidate palettes A/B/C; A won. Kept for reference.
- `moodboard/` — Thai + international reference captures with notes. Raw retry captures moved to `handbook/archive/design-captures/`.
